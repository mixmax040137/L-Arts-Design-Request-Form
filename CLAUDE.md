# บริบทโครงการสำหรับ Claude Code

ไฟล์นี้มีไว้ให้เซสชันใหม่ (โดยเฉพาะเมื่อย้ายมาทำงานบนเครื่องตัวเอง) เข้าใจโครงการได้ทันทีโดยไม่ต้องอ่านโค้ดทั้งหมด

## โครงการนี้คืออะไร

ระบบขอรับบริการออกแบบสื่อประชาสัมพันธ์ ของฝ่ายสื่อสารองค์กร คณะศิลปศาสตร์ มหาวิทยาลัยธรรมศาสตร์
สร้างด้วย **Google Apps Script** ล้วน โดยใช้ **Google Sheets เป็นฐานข้อมูล** และ **Google Drive เก็บไฟล์งาน**
ทั้งระบบรันภายใต้บัญชี `pr@arts.tu.ac.th` ไม่มีเซิร์ฟเวอร์อื่น ไม่มี build step

ที่มาของความต้องการอยู่ใน Workshop Canvas ของผู้ใช้ ปัญหาหลักที่ระบบต้องแก้คือ
1. ข้อมูลคำขอไม่ครบตั้งแต่ต้นทาง ทำให้ต้องประสานงานย้อนกลับหลายรอบ
2. ไม่มีทะเบียนงานกลางและระบบติดตามสถานะที่ผู้ขอตรวจสอบเองได้

## คำสั่งที่ใช้บ่อย

```bash
npm test            # ชุดทดสอบ 149 รายการ ต้องผ่านทั้งหมดก่อน commit เสมอ
./setup.sh          # อัปโหลดโค้ดขึ้น Apps Script (Windows ใช้ .\setup.ps1)
```

ไม่มี linter และไม่มี build step โค้ดที่เขียนคือโค้ดที่รันจริง

## กติกาสำคัญของโค้ดฐานนี้

- **ภาษาไทยทั้งหมด** — คอมเมนต์ ข้อความ error ที่ผู้ใช้เห็น และ UI เป็นภาษาไทย
  ข้อความ error ที่ผู้ใช้เห็นต้องบอกวิธีแก้ ไม่ใช่แค่บอกว่าผิด
- **หลีกเลี่ยงไวยากรณ์ใหม่ในไฟล์ `.gs`** — ใช้ `var` และ `function` ธรรมดา ไม่ใช้ `?.` `??`
  (ฝั่ง `.html` ที่รันในเบราว์เซอร์ใช้ ES ใหม่ได้)
- **ห้ามอ้างถึงตัวแปร top-level ข้ามไฟล์ตอนโหลด** — Apps Script ต่อไฟล์เข้าด้วยกันโดยลำดับไม่แน่นอน
  การอ้างอิงข้ามไฟล์ต้องอยู่ในตัวฟังก์ชันเท่านั้น
- **ฟังก์ชันลงท้ายด้วย `_`** = ใช้ภายใน เรียกจาก `google.script.run` ไม่ได้
  ฟังก์ชันที่ขึ้นต้นด้วย `api` = จุดเชื่อมกับหน้าเว็บ ทุกตัวต้องห่อด้วย `respond_()`
  และตัวที่เป็นของเจ้าหน้าที่ต้องเรียก `requireAuth_(token)` หรือ `requireAdmin_(token)` เสมอ
- **การเขียนที่ออกเลขหรือเปลี่ยนสถานะต้องอยู่ใน `withLock_()`**
- **ข้อความจากผู้ใช้ต้องผ่าน `escapeHtml_()` หรือ `App.esc()` ก่อนแสดงผลเสมอ** ทั้งในเว็บและอีเมล
- **แก้โครงสร้างคอลัมน์ต้องแก้ที่ `COLUMNS` ใน `00_Config.gs` เท่านั้น** แล้วรัน `setupSystem()` ใหม่

## แผนผังไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `00_Config.gs` | ค่าคงที่ สถานะ ประเภทสื่อ โครงสร้างคอลัมน์ สีแบรนด์ |
| `01_Utils.gs` | ตัวช่วยล้วน ไม่มี side effect (วันที่ไทย ตรวจข้อมูล HMAC escape) |
| `02_Store.gs` | ชั้นเข้าถึงชีตเท่านั้น (`readAll_` `findBy_` `insert_` `update_` `withLock_` `nextCounter_`) |
| `03_Setup.gs` | ติดตั้งระบบ ติดตั้งอัตโนมัติครั้งแรก สร้างบัญชี รหัสติดตั้ง |
| `04_Requests.gs` | ตรรกะคำขอ ตรวจข้อมูล เปลี่ยนสถานะ รอบแก้ไข มุมมองสาธารณะ |
| `05_Files.gs` | Google Drive ตั้งชื่อไฟล์มาตรฐาน คลังผลงาน |
| `06_Mailer.gs` | เทมเพลตอีเมลทุกชนิด |
| `07_AI.gs` | เรียก Claude API สรุป Design Brief + งานเบื้องหลัง `workerTick` |
| `08_Auth.gs` | แฮชรหัสผ่าน token สิทธิ์ จำกัดการล็อกอิน |
| `09_Stats.gs` | สถิติสำหรับแดชบอร์ด |
| `10_WebApp.gs` | `doGet` และ `api*` ทั้งหมด |
| `page_App.html` | โครงหน้าเว็บ (มาร์กอัปของทุกหน้าอยู่ที่นี่) |
| `ui_Style.html` | CSS ทั้งหมด |
| `ui_Script_Core.html` | `App` (เรียก API สลับหน้า แจ้งเตือน) และ `Setup` (ตั้งค่าครั้งแรก) |
| `ui_Script_Form.html` | `Form` แบบฟอร์ม 3 ขั้นตอน |
| `ui_Script_Track.html` | `Track` หน้าติดตามของผู้ขอ |
| `ui_Script_Admin.html` | `Admin` คิวงาน แดชบอร์ด คลังผลงาน ตั้งค่า |

## สถานะงาน

`NEW` รับคำขอ → `DESIGNING` อยู่ระหว่างออกแบบ → `REVIEW` รอตรวจร่าง → `REVISING` อยู่ระหว่างแก้ไข → `DELIVERED` ส่งมอบแล้ว
สถานะยกเว้น `INFO_NEEDED` รอข้อมูลเพิ่มเติม และ `CANCELLED` ยกเลิก
เส้นทางที่อนุญาตกำหนดไว้ที่ `STATUS_TRANSITIONS` ห้ามข้ามขั้น

## การทดสอบ

`tests/mock-gas.js` จำลอง Apps Script ทั้งหมด (Sheets, Drive, Gmail, Properties, Cache, Lock, Triggers, UrlFetch)
`tests/run.js` โหลดไฟล์ `.gs` ทุกไฟล์เข้า vm แล้วทดสอบจริง

**เพิ่มฟีเจอร์ใหม่ต้องเพิ่มเทสต์เสมอ** โดยเฉพาะกรณีสิทธิ์การเข้าถึงและกรณีข้อมูลผิดรูปแบบ
ถ้าเทสต์ล้มเหลว ให้ตรวจก่อนว่าเป็นบั๊กจริงหรือเป็นข้อจำกัดของตัวจำลอง อย่าแก้เทสต์ให้ผ่านเฉย ๆ

## การเรียก Claude API

อยู่ใน `07_AI.gs` ใช้ `UrlFetchApp` ตรง (Apps Script ไม่มี SDK) โมเดล `claude-opus-5`
ใช้ structured output (`output_config.format` แบบ `json_schema`) และ `fallbacks: "default"`
พร้อม beta header `server-side-fallback-2026-07-01`
ต้องเช็ค `stop_reason === 'refusal'` ก่อนอ่าน content เสมอ
ถ้า API ล้มเหลวต้องตกไปใช้ `fallbackBrief_()` ไม่ปล่อยให้ระบบพัง

## สิ่งที่ยังไม่ได้ทำ

- ยังไม่เคยรันบน Google จริง (ทดสอบผ่าน mock ทั้งหมด) — รอผู้ใช้ deploy แล้วทดสอบตาม checklist ใน `docs/DEPLOY.md`
- ยังไม่มีการทดสอบ UI อัตโนมัติ (ฝั่ง `.html` ตรวจแค่ syntax และการอ้างอิงฟังก์ชัน)
